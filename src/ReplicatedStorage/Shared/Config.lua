-- Config.lua (DEPRECATED — use GameConfig)
-- Redireciona para GameConfig para manter compatibilidade
local ReplicatedStorage = game:GetService("ReplicatedStorage")
return require(ReplicatedStorage.Shared.GameConfig)
